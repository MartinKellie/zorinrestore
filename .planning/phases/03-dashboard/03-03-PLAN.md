---
phase: 03-dashboard
plan: "03"
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/actions/review.ts
  - lib/actions/secrets.ts
  - tests/review.test.ts
  - tests/secrets.test.ts
autonomous: true
requirements: [REVQ-01, REVQ-02, REVQ-03, SEC-02, SEC-03, SEC-04]
must_haves:
  truths:
    - "classifyReviewItem sets needs_review=false and importance on the item when valid"
    - "classifyReviewItem rejects any value outside Essential/Useful/Optional/Ignore"
    - "addSecretReminder rejects empty name and returns error"
    - "addSecretReminder inserts row via adminSupabase with user_id from authenticated session"
    - "flagSecretDep updates has_secret_dep boolean on the scan_item via adminSupabase"
    - "deleteSecretReminder removes the reminder by id, verifying ownership first"
    - "npx vitest run tests/review.test.ts tests/secrets.test.ts shows all tests GREEN"
  artifacts:
    - path: "lib/actions/review.ts"
      provides: "classifyReviewItem Server Action"
      exports: ["classifyReviewItem"]
    - path: "lib/actions/secrets.ts"
      provides: "addSecretReminder, deleteSecretReminder, flagSecretDep Server Actions"
      exports: ["addSecretReminder", "deleteSecretReminder", "flagSecretDep"]
    - path: "tests/review.test.ts"
      provides: "Unit tests for REVQ-02"
    - path: "tests/secrets.test.ts"
      provides: "Unit tests for SEC-02, SEC-03"
  key_links:
    - from: "lib/actions/review.ts"
      to: "adminSupabase"
      via: "update scan_items SET needs_review=false, importance=classification"
      pattern: "needs_review.*false"
    - from: "lib/actions/secrets.ts"
      to: "adminSupabase"
      via: "insert into secret_reminders"
      pattern: "secret_reminders.*insert"
---

<objective>
Implement Review Queue and Secrets Checklist Server Actions — the write path for classifying review items and managing manual secret reminders.

Purpose: classifyReviewItem moves items from the Review Queue into the main inventory (clears needs_review). The secrets Server Actions manage the secret_reminders table (added in migration 0003) and flag scan_items with has_secret_dep.

Output: lib/actions/review.ts + lib/actions/secrets.ts, all tests GREEN.
</objective>

<execution_context>
@/home/martin/.claude/get-shit-done/workflows/execute-plan.md
@/home/martin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-dashboard/03-RESEARCH.md
@.planning/phases/03-dashboard/03-01-SUMMARY.md

<interfaces>
<!-- secret_reminders table (created in migration 0003):
  id uuid PK, user_id uuid FK auth.users, name text NOT NULL check length > 0,
  note text (nullable), created_at timestamptz
  RLS: owner-only select/insert/delete
  GRANT: authenticated role has full CRUD
-->

<!-- scan_items.has_secret_dep: boolean default false (added in migration 0003) -->

<!-- Server Action pattern (same as lib/actions/inventory.ts in plan 03-02):
  "use server"
  createClient().auth.getUser() → { error: "Unauthorized" } if null
  RLS ownership check via createClient().from() SELECT → { error: "Not found" } if null
  adminSupabase write
  return {} | { error: string }
-->

<!-- For addSecretReminder: insert needs user_id from the authenticated user.
  Use adminSupabase.from("secret_reminders").insert({ user_id: user.id, name, note })
  No need for ownership SELECT before insert — user_id is set from session.
-->

<!-- For deleteSecretReminder: verify ownership by selecting the reminder via
  createClient() (RLS will only return the row if it belongs to the user),
  then delete via adminSupabase.
-->
</interfaces>
</context>

<feature>
  <name>Review Queue and Secrets Server Actions</name>
  <files>lib/actions/review.ts, lib/actions/secrets.ts, tests/review.test.ts, tests/secrets.test.ts</files>
  <behavior>
    Review Queue:
    - classifyReviewItem("id", "Essential") → {} when user owns item; sets importance="Essential", needs_review=false
    - classifyReviewItem("id", "BadValue") → { error: "Invalid classification" }
    - classifyReviewItem("id", "Essential") when unauthenticated → { error: "Unauthorized" }
    - classifyReviewItem("id", "Essential") when item not found → { error: "Item not found" }

    Secrets:
    - addSecretReminder("") → { error: "Name is required" }
    - addSecretReminder("OPENAI_API_KEY") when unauthenticated → { error: "Unauthorized" }
    - addSecretReminder("OPENAI_API_KEY") → {} when authenticated; inserts row with user_id
    - addSecretReminder("OPENAI_API_KEY", "my note") → {} with optional note
    - deleteSecretReminder("id") when unauthenticated → { error: "Unauthorized" }
    - deleteSecretReminder("id") → {} when user owns reminder
    - flagSecretDep("item-id", true) when unauthenticated → { error: "Unauthorized" }
    - flagSecretDep("item-id", true) → {} when user owns item; sets has_secret_dep=true
    - flagSecretDep("item-id", false) → {} when user owns item; sets has_secret_dep=false
  </behavior>
  <implementation>
    RED: Update tests/review.test.ts and tests/secrets.test.ts stubs (from plan 03-01) to
    contain the full real expectations. Tests will fail because the action modules don't exist.

    GREEN lib/actions/review.ts:
    - "use server"
    - ClassificationSchema = z.enum(["Essential", "Useful", "Optional", "Ignore"])
    - classifyReviewItem(itemId: string, classification: string): Promise<{ error?: string }>
      1. Validate with ClassificationSchema.safeParse
      2. getUser() → return { error: "Unauthorized" } if null
      3. SELECT scan_items by id via createClient() → return { error: "Item not found" } if null
      4. adminSupabase UPDATE scan_items SET importance=parsed.data, needs_review=false, updated_at WHERE id
      5. Return {} or { error }

    GREEN lib/actions/secrets.ts:
    - "use server"
    - addSecretReminder(name: string, note?: string): Promise<{ error?: string }>
      1. if (!name || name.trim().length === 0) return { error: "Name is required" }
      2. getUser() → return { error: "Unauthorized" } if null
      3. adminSupabase INSERT secret_reminders { user_id: user.id, name: name.trim(), note: note ?? null }
      4. Return {} or { error }
    - deleteSecretReminder(reminderId: string): Promise<{ error?: string }>
      1. getUser() → return { error: "Unauthorized" } if null
      2. SELECT secret_reminders by id via createClient() (RLS ownership check)
      3. adminSupabase DELETE secret_reminders WHERE id=reminderId
      4. Return {} or { error }
    - flagSecretDep(itemId: string, value: boolean): Promise<{ error?: string }>
      1. getUser() → return { error: "Unauthorized" } if null
      2. SELECT scan_items by id via createClient() → return { error: "Item not found" } if null
      3. adminSupabase UPDATE scan_items SET has_secret_dep=value, updated_at WHERE id
      4. Return {} or { error }

    DO NOT use getSession(). DO NOT inline adminSupabase in client-facing code.
  </implementation>
</feature>

<verification>
  <automated>cd /home/martin/Projects/zorinpackages && npx vitest run tests/review.test.ts tests/secrets.test.ts --reporter=verbose 2>&1 | tail -25</automated>
</verification>

<success_criteria>
npx vitest run tests/review.test.ts tests/secrets.test.ts passes all tests GREEN. Both lib/actions/review.ts and lib/actions/secrets.ts exist with all exported functions. Full suite (npx vitest run) remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-03-SUMMARY.md`
</output>
