"use client"

// Placeholder — Cursor will replace this via a future UI spec
export function ReviewView({ items }: { items: Record<string, unknown>[] }) {
  return <div>Review Queue UI coming soon</div>
}
