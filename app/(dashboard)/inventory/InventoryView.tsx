"use client"

// Placeholder — Cursor will replace this via INVENTORY_UI_SPEC.md
export function InventoryView({
  items,
  machine,
  latestRun,
}: {
  items: Record<string, unknown>[]
  machine: Record<string, unknown> | null
  latestRun: Record<string, unknown> | null
}) {
  return <div>Inventory UI coming soon</div>
}
